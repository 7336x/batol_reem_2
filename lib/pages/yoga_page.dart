import 'package:flutter/material.dart';

class YogaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Yoga Page'),
      ),
      body: Center(
        child: Text('Welcome to the Yoga Page!'),
      ),
    );
  }
}
